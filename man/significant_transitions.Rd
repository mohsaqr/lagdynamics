% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-transitions.R
\name{significant_transitions}
\alias{significant_transitions}
\alias{significant_transitions.lsa}
\alias{significant_transitions.lsa_group}
\title{Filter Transitions by Significance}
\usage{
significant_transitions(fit, ...)

\method{significant_transitions}{lsa}(fit, alpha = NULL, ...)

\method{significant_transitions}{lsa_group}(fit, alpha = NULL, ...)
}
\arguments{
\item{fit}{An \code{lsa} object returned by \code{\link[=lsa]{lsa()}}.}

\item{alpha}{Numeric scalar in \verb{(0, 1)}. Significance threshold.
Default \code{NULL}, which uses the alpha snapshotted on the fit
(\code{fit$params$alpha}).}
}
\value{
A data.frame with the same columns as \code{fit$edges},
containing only the significant rows. Row order is preserved.
}
\description{
Returns the subset of \code{fit$edges} whose adjusted-residual p-value
falls below \code{alpha}. Cells with non-finite p-values (structural
zeros, zero-margin rows, or non-estimable cells) are excluded.
}
\examples{
fit <- lsa(engagement, engine = "classical")
significant_transitions(fit)
significant_transitions(fit, alpha = 0.01)

For a grouped fit (`lsa_group`), the filter is applied within each
group and the results are row-bound into one data.frame with a
leading `group` column.

}
\seealso{
\code{\link[=overrepresented_transitions]{overrepresented_transitions()}},
\code{\link[=underrepresented_transitions]{underrepresented_transitions()}}, \code{\link[=common_transitions]{common_transitions()}}
}
