% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-transitions.R
\name{underrepresented_transitions}
\alias{underrepresented_transitions}
\alias{underrepresented_transitions.lsa}
\alias{underrepresented_transitions.lsa_group}
\title{Filter Underrepresented (Negative-Residual) Transitions}
\usage{
underrepresented_transitions(fit, ...)

\method{underrepresented_transitions}{lsa}(fit, alpha = NULL, ...)

\method{underrepresented_transitions}{lsa_group}(fit, alpha = NULL, ...)
}
\arguments{
\item{fit}{An \code{lsa} object returned by \code{\link[=lsa]{lsa()}}.}

\item{alpha}{Numeric scalar in \verb{(0, 1)}. Significance threshold.
Default \code{NULL}, which uses the alpha snapshotted on the fit
(\code{fit$params$alpha}).}
}
\value{
A data.frame with the same columns as \code{fit$edges}.
}
\description{
Returns significant transitions where the observed count is below
the expected count under independence (negative adjusted residual).
These are the cells where the focal transition is actively avoided
relative to chance.
}
\examples{
fit <- lsa(engagement, engine = "classical")
underrepresented_transitions(fit)

}
\seealso{
\code{\link[=significant_transitions]{significant_transitions()}},
\code{\link[=overrepresented_transitions]{overrepresented_transitions()}}
}
