% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tna-bridge.R
\name{as.igraph.lsa}
\alias{as.igraph.lsa}
\title{Convert an lsa Fit to an igraph Graph}
\usage{
\method{as.igraph}{lsa}(
  x,
  weights = c("prob", "count", "adj_res", "lift"),
  positive_residuals_only = TRUE,
  mode = "directed",
  ...
)
}
\arguments{
\item{x}{An \code{lsa} object returned by \code{\link[=lsa]{lsa()}}.}

\item{weights}{Character. As in \code{\link[=lsa_to_tna]{lsa_to_tna()}}.}

\item{positive_residuals_only}{Logical. As in \code{\link[=lsa_to_tna]{lsa_to_tna()}}.}

\item{mode}{Character. Passed to \code{\link[igraph:graph_from_adjacency_matrix]{igraph::graph_from_adjacency_matrix()}}.
Default \code{"directed"}; use \code{"undirected"} for bidirectional fits.}

\item{...}{Unused.}
}
\value{
An \code{igraph} graph with \code{weight} edge attribute.
}
\description{
Wraps the fit's chosen weight matrix in an \code{igraph} graph. Requires
the \code{igraph} package (declared in Suggests).
}
\examples{
\dontrun{
fit <- lsa(engagement, engine = "classical")
g <- as.igraph(fit, weights = "prob")
igraph::betweenness(g)
}

}
