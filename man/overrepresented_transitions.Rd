% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-transitions.R
\name{overrepresented_transitions}
\alias{overrepresented_transitions}
\alias{overrepresented_transitions.lsa}
\alias{overrepresented_transitions.lsa_group}
\title{Filter Overrepresented (Positive-Residual) Transitions}
\usage{
overrepresented_transitions(fit, ...)

\method{overrepresented_transitions}{lsa}(fit, alpha = NULL, ...)

\method{overrepresented_transitions}{lsa_group}(fit, alpha = NULL, ...)
}
\arguments{
\item{fit}{An \code{lsa} object returned by \code{\link[=lsa]{lsa()}}.}

\item{alpha}{Numeric scalar in \verb{(0, 1)}. Significance threshold.
Default \code{NULL}, which uses the alpha snapshotted on the fit
(\code{fit$params$alpha}).}
}
\value{
A data.frame with the same columns as \code{fit$edges}.
}
\description{
Returns significant transitions where the observed count exceeds
the expected count under independence (i.e. positive adjusted
residual). These are the cells where the focal transition occurs
more often than chance.
}
\examples{
fit <- lsa(engagement, engine = "classical")
overrepresented_transitions(fit)

}
\seealso{
\code{\link[=significant_transitions]{significant_transitions()}},
\code{\link[=underrepresented_transitions]{underrepresented_transitions()}}
}
