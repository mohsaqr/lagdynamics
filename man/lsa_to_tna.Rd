% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tna-bridge.R
\name{lsa_to_tna}
\alias{lsa_to_tna}
\alias{lsa_to_tna.lsa}
\alias{lsa_to_tna.lsa_group}
\title{Convert an lsa Fit to a tna Network}
\usage{
lsa_to_tna(x, ...)

\method{lsa_to_tna}{lsa}(x, weights = c("prob", "count", "adj_res", "lift"), ...)

\method{lsa_to_tna}{lsa_group}(x, weights = c("prob", "count", "adj_res", "lift"), ...)
}
\arguments{
\item{x}{An \code{lsa} fit from \code{\link[=lsa]{lsa()}}, or an \code{lsa_group} from
\code{lsa(..., group = )}.}

\item{...}{Method-specific arguments.}

\item{weights}{Character. Which matrix to expose as the tna edge
weights. One of \code{"prob"} (row-normalised probabilities, default),
\code{"count"} (raw observed counts), \code{"adj_res"} (adjusted residuals,
residual network), or \code{"lift"} (observed / expected, association
strength).
\code{tna} requires non-negative weights, so the \code{"adj_res"} choice
always clips negative residuals to \code{0} (returning the
over-representation network). To work with signed residuals, read
\code{fit$adj_res} directly.}
}
\value{
For an \code{lsa} fit, a \code{tna} object with \code{weights}, \code{inits},
\code{labels}, \code{data} slots and \code{type}/\code{scaling}/\code{class} attributes
(\code{type} is \code{"frequency"} for counts, \code{"relative"} otherwise). For
an \code{lsa_group}, a \code{group_tna} (named list of \code{tna} objects).

The \code{data}/\code{inits} slots (the sequences tna resamples from) are
attached only for \code{weights = "prob"} or \code{"count"}, which tna can
re-estimate faithfully. For \code{"adj_res"} and \code{"lift"} they are
omitted with a warning, because tna's resampling verbs would
re-estimate a probability network rather than the residual/lift
scale carried in \code{weights}; the non-resampling verbs
(\code{centralities}, \code{prune}, ...) still work.
}
\description{
Convert an \code{lsa} fit to a \code{tna}-class network object usable by the
\code{tna} package's centrality, pruning, community, and bootstrap
routines. Requires the \code{tna} package (declared in Suggests).
}
\details{
The function is deliberately \strong{not} named \code{as_tna}, to avoid
overlapping export names with other packages so they can be loaded
together without masking each other. \code{lsa_to_tna()} is lagseq's own,
collision-free converter.
}
\examples{
\dontrun{
fit <- lsa(engagement, engine = "classical")
net <- lsa_to_tna(fit, weights = "prob")
tna::centralities(net)
tna::prune(net, method = "threshold", threshold = 0.05)
}

}
