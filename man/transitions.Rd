% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-transitions.R
\name{transitions}
\alias{transitions}
\alias{transitions.lsa}
\alias{transitions.lsa_group}
\title{Transitions of an LSA Fit (Tidy)}
\usage{
transitions(
  fit,
  significant = FALSE,
  direction = c("any", "over", "under"),
  min_count = NULL,
  alpha = NULL,
  sort = c("none", "strength", "count", "prob")
)

\method{transitions}{lsa}(
  fit,
  significant = FALSE,
  direction = c("any", "over", "under"),
  min_count = NULL,
  alpha = NULL,
  sort = c("none", "strength", "count", "prob")
)

\method{transitions}{lsa_group}(
  fit,
  significant = FALSE,
  direction = c("any", "over", "under"),
  min_count = NULL,
  alpha = NULL,
  sort = c("none", "strength", "count", "prob")
)
}
\arguments{
\item{fit}{An \code{lsa} fit from \code{\link[=lsa]{lsa()}}, or a grouped \code{lsa_group}.}

\item{significant}{Logical. Keep only transitions whose adjusted-
residual p-value is below \code{alpha}. Default \code{FALSE} (keep all).}

\item{direction}{One of \code{"any"} (default), \code{"over"}
(over-represented: significant with a positive residual), or
\code{"under"} (under-represented: significant with a negative
residual). Selecting a direction implies \code{significant = TRUE}.}

\item{min_count}{Optional integer. Keep only transitions observed at
least this many times. Default \code{NULL} (no count filter).}

\item{alpha}{Significance threshold. Default \code{NULL}, which uses the
alpha recorded on the fit (\code{fit$params$alpha}).}

\item{sort}{Row ordering. \code{"none"} (default) keeps the matrix
(column-major) order; \code{"strength"} orders by \verb{|adj_res|}, \code{"count"} by
observed count, \code{"prob"} by transition probability -- each descending,
so the table reads strongest-first.}
}
\value{
A \code{data.frame}, one row per transition, with columns \code{from},
\code{to} (the source and target \strong{state names}), \code{lag}, \code{count},
\code{expected}, \code{prob} (row-conditional), \code{prob_col} (column-
conditional), \code{adj_res}, \code{p}, \code{yules_q}, \code{kappa}, \code{kappa_z},
\code{kappa_p}, \code{lift}, \code{sign}, \code{significant}. Engines that compute extra
per-cell statistics append them as further columns (e.g. the two-cell
engine adds \code{odds_ratio}, \code{log_or}, \code{log_or_se}). A grouped fit gains a
leading \code{group} column. Row names are reset.
}
\description{
The canonical way to read a fit's transitions as a tidy
one-row-per-transition \code{data.frame}. \code{transitions(fit)} returns every
transition; the arguments narrow it.
}
\examples{
fit <- lsa(group_regulation)
transitions(fit)                       # all transitions
transitions(fit, significant = TRUE)   # significant ones
transitions(fit, direction = "over")   # over-represented
transitions(fit, min_count = 500)      # frequently observed

}
\seealso{
\code{\link[=lsa]{lsa()}}, \code{\link[=nodes]{nodes()}}, \code{\link[=tests]{tests()}}
}
