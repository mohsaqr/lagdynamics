% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter-transitions.R
\name{common_transitions}
\alias{common_transitions}
\alias{common_transitions.lsa}
\alias{common_transitions.lsa_group}
\title{Filter Frequently-Observed Transitions}
\usage{
common_transitions(fit, ...)

\method{common_transitions}{lsa}(fit, min_count = 1L, ...)

\method{common_transitions}{lsa_group}(fit, min_count = 1L, ...)
}
\arguments{
\item{fit}{An \code{lsa} object returned by \code{\link[=lsa]{lsa()}}.}

\item{min_count}{Integer scalar \verb{>= 1}. Minimum observed count.
Default \code{1L} (keep every transition that was observed at least
once).}
}
\value{
A data.frame with the same columns as \code{fit$edges}.
}
\description{
Returns transitions whose observed count is at least \code{min_count}.
This is a descriptive volume filter independent of statistical
significance, useful for trimming long-tail noise from a transition
network before visualisation.
}
\examples{
fit <- lsa(engagement, engine = "classical")
common_transitions(fit, min_count = 3)

}
\seealso{
\code{\link[=significant_transitions]{significant_transitions()}}
}
